import { SpecificUsersPipe } from './specific-users.pipe';

describe('SpecificUsersPipe', () => {
  it('create an instance', () => {
    const pipe = new SpecificUsersPipe();
    expect(pipe).toBeTruthy();
  });
});
